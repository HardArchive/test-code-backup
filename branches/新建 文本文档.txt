配置文件规则如下:
1、DC段存放分发中心服务器IP和端口
2、PB段存放公安后台服务器IP和端口
3、SITENUM段对应总所有的解析网站和数量
4、SITE+数字段对应解析网址和其对应的正则表达式



客户端主要流程如下：
1、启动
2、读取配置文件(分发中心和公安后台IP和端口、解析网站数量，网址、正则表达式)
3、利用取得的IP端口连接分发中心服务器
3、请求需解析的IP段(发送UpLoadHead包，dwCommand命令号为1)
4、接收分发中心的返回的IP段数据(IPSection, 命令类型为2)



配置文件规则如下:
1、DC段存放分发中心服务器IP和端口
2、PB段存放公安后台服务器IP和端口
3、SITENUM段对应总所有的解析网站和数量
4、SITE+数字段对应解析网址和其对应的正则表达式

二

客户端主要流程如下：
1、启动
2、读取配置文件(分发中心和公安后台IP和端口、解析网站数量，网址、正则表达式)
3、利用取得的IP端口连接分发中心服务器
3、请求需解析的IP段(发送UpLoadHead包，dwCommand命令号为1)
4、接收分发中心的返回的IP段数据(IPSection, 命令类型为2)




配置文件规则如下:
1、DC段存放分发中心服务器IP和端口
2、PB段存放公安后台服务器IP和端口
3、SITENUM段对应总所有的解析网站和数量
4、SITE+数字段对应解析网址和其对应的正则表达式


三
客户端主要流程如下：
1、启动
2、读取配置文件(分发中心和公安后台IP和端口、解析网站数量，网址、正则表达式)
3、利用取得的IP端口连接分发中心服务器
3、请求需解析的IP段(发送UpLoadHead包，dwCommand命令号为1)
4、接收分发中心的返回的IP段数据(IPSection, 命令类型为2)




配置文件规则如下:
1、DC段存放分发中心服务器IP和端口
2、PB段存放公安后台服务器IP和端口
3、SITENUM段对应总所有的解析网站和数量
4、SITE+数字段对应解析网址和其对应的正则表达式

四

客户端主要流程如下：
1、启动
2、读取配置文件(分发中心和公安后台IP和端口、解析网站数量，网址、正则表达式)
3、利用取得的IP端口连接分发中心服务器
3、请求需解析的IP段(发送UpLoadHead包，dwCommand命令号为1)
4、接收分发中心的返回的IP段数据(IPSection, 命令类型为2)




配置文件规则如下:
1、DC段存放分发中心服务器IP和端口
2、PB段存放公安后台服务器IP和端口
3、SITENUM段对应总所有的解析网站和数量
4、SITE+数字段对应解析网址和其对应的正则表达式


五
客户端主要流程如下：
1、启动
2、读取配置文件(分发中心和公安后台IP和端口、解析网站数量，网址、正则表达式)
3、利用取得的IP端口连接分发中心服务器
3、请求需解析的IP段(发送UpLoadHead包，dwCommand命令号为1)
4、接收分发中心的返回的IP段数据(IPSection, 命令类型为2)





